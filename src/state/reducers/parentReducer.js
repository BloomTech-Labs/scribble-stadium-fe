const initialState = {
  id: null,
  name: null,
  email: null,
  children: [],
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
