const initialState = {
  id: null,
  name: null,
  isDyslexic: null,
  avatarUrl: null,
  gradeLevel: null,
  parentId: null,
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
